grades = [10,40,50,60]


grades.append(95)

average_grade = sum(grades) / len(grades)
print(f"average grade is: {average_grade:.3f}")

higest_grade = print(max(grades))
    
lowest_grade = min(grades)
